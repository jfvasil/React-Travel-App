import PageHeading from "../components/PageHeading"

const FindResults = () => {
    const dest = 'placeholder'
  return (
    <div>
      <PageHeading text={`You should go to ${dest}!!`} />
    </div>
  )
}

export default FindResults